// Libraries
import styled from 'styled-components';

// Components
import { Slide } from 'hero-slider';

const StyledSlide = styled(Slide)`
  .slide-wrapper {
    background-color: rgba(0, 157.5, 225.5, 0.5);
  }
`;

export default StyledSlide;
